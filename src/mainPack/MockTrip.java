package mainPack;

import mainPack.NotSureWhatToName.Colour;

import java.lang.reflect.Array;
import java.util.ArrayList;
import java.util.Scanner;


public class MockTrip {
    /*

    Scanner _scanner = new Scanner(System.in);
    StartUp startUp = new StartUp();
    ArrayList<Colour> colourArrayList = startUp.createColours();

    Colour[] trackArray = new Colour[30];

    public void newMockTrip () {

        // Selects index position
        System.out.println("Select an index position");
        int index = inScan()-1;

        // Selects a colour
        System.out.println("Select a colour: ");
        for(int i = 0; i < colourArrayList.size(); i++) {
            System.out.println((i+1) + ". " + colourArrayList.get(i).toString());
        }

        trackArray[index] = colourArrayList.get(inScan()-1);

        printArray();
    }

    public int inScan() {
        return Integer.parseInt(_scanner.nextLine().trim());
    }

    public void printArray() {
        for(int i = 0; i < trackArray.length; i++)
        System.out.println(i+1 + " [" + trackArray[i] + "]");
    }

     */
}
